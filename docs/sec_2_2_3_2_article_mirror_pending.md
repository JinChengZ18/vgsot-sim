# 待应用：article/chapter02.md 的 §2.2.3.2 镜像修订

> **✅ 已于 2026-07-05 直接应用到 `article/chapter02.md`**（用户许可）：§2.2.3.2 的 norm 软化 + 收敛性重写 + E 讨论 + 核验表均已写入。校准部分（θ_SH/图注）**未动**——并行会话已正确处理为 0.066 / 写入工况~1% 匹配（含 `[^note-dev-thetacalib]` 试错 footnote）。文件未提交（并行会话仍在改 §2.4；watcher 自动出 docx）。以下为存档留底。

> 本轮已完成 `00章节撰写/2_2_磁学仿真方法与平台_merged.md`（学位论文正文）的 §2.2.3.2/§2.2.4.6 修订，但 `article/chapter02.md`（期刊交付版，watcher 自动出 docx）在编辑当时正被并行会话改动（§2.4 储备池，mtime 持续推进），为避免覆盖其工作**未同步**。待该文件静默后按下列替换应用（已适配期刊版约定：ASCII 括号带空格、脚注式引用 `[^ref-*]`、自洽不引「见§x」）。

## 替换 1 —— 保模长段（原 line 739 末句）
**原文**：「…因此，无论时间步长 $$\Delta t$$ 取何值，都在数学上严格保证 $$|\mathbf{m}_{n+1}| = |\mathbf{m}_n| = 1$$，彻底消除了坐标奇点并排除了手动归一化的需要。」
**改为**：「…因此，无论时间步长 $$\Delta t$$ 取何值，该闭式更新都在浮点精度内保持 $$|\mathbf{m}_{n+1}| = |\mathbf{m}_n| = 1$$，消除了坐标奇点；实现中保留的一步归一化仅清除 $$O(\varepsilon)$$ 舍入残差、非算法所必需。」

## 替换 2 —— 收敛性分析段（原 line 741 整段，含虚假的「隐式中点二阶 + Boltzmann 无修正」）
**改为**（自洽、含 euler T/2 对照，无「见§」交叉引用）：

> **收敛性分析。** 上式旋转矢量 $$\mathbf{w}_n$$ 在左端点 $$\mathbf{m}_n$$ 处单次取值，该更新 $$\mathbf{m}_{n+1}=\mathbf{A}_n\mathbf{m}_n$$ 为显式-$$\mathbf{w}$$ 几何积分格式：Cayley 变换的正交性仅取决于 $$\mathbf{\Omega}_n$$ 的反对称性，故模长守恒对任意 $$\Delta t$$ 严格成立，但时间精度受 $$\mathbf{w}$$ 冻结的限制、在确定性极限下为全局一阶；将 $$\mathbf{w}$$ 改在中点 $$(\mathbf{m}_n+\mathbf{m}_{n+1})/2$$ 处自洽取值 ( 数次不动点迭代 ) 即还原经典隐式中点法的二阶对称性。本文以自收敛实验核验：关闭热噪声扫描 $$\Delta t$$，发布内核全局阶实测为 $$1.01$$、迭代中点格式为 $$2.00$$，后者已作为可选积分器 `cayley_midpoint` 提供。对含 Stratonovich 白噪声的 sLLG 方程须区分强收敛与弱收敛[^ref-kloeden-platen]：本文在噪声主导工况下以共享布朗路径耦合实测显式格式的强收敛阶为 $$0.97$$ ( $$95\%$$ 置信区间 $$[0.95,1.00]$$ )、弱收敛阶为 $$1.98\text{–}2.04$$ ( 横向对称破缺后仍 $$\ge1.8$$ )，后者决定统计均值的步长精度，构成大步长宽容度的实测依据。$$60\,\mathrm{ns}$$ 零驱动弛豫给出有效温度 $$T_{\mathrm{eff}}/T=0.979\pm0.014$$，即该格式在 $$1\text{–}2\%$$ 内收敛于正确的 Boltzmann 分布而无需 Itô-Stratonovich 修正[^ref-daquino-midpoint]；作为对照，球坐标-Euler 步因缺失 Wong-Zakai 噪声漂移而系统性采样于 $$T/2$$，表明保结构积分是随机热力学一致性的必要条件。

## 期刊版 §2.2.4.6 / 参数表 / 图注（若期刊版含对应段落）
按学位论文正文同口径处理：θ_SH 记 $$0.066$$、$$I_{50}\approx1290\,\mu\mathrm{A}$$（较实验 1152 µA 偏高约 12%），补 euler T/2 (Wong-Zakai) 缺陷条目，删除「匹配 1%/5%」等陈旧声称并标注重标定为待办。注意：期刊交付版按规范剥离章节交叉引用（「见§2.2.4.6」需自洽重写或转脚注）。

## 复现与佐证
所有数字见 `docs/sec_2_2_3_2_experiment_plan.md` 与 `result/sec_2_2_3_2/`（脚本已入库、产物 gitignore）。fig 2.11 积分器对照数据：`result/sec_2_2_3_2/fig211/fig211_compare_summary.json`（euler I_50=1289.4、cayley I_50=1291.5 µA）。
